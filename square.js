/* write a program to calculate square of number using user defined function*/

function calcSquare(a) {
  let square = a * a;
  return square;
}

let square = calcSquare(3);
console.log(`${square}`);
